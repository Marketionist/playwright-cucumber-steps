{
  "name": "playwright-cucumber-steps",
  "version": "1.2.0",
  "description": "Cucumber steps (step definitions) written with Playwright for end-to-end (e2e) tests",
  "engines": {
    "node": ">=22.3.x"
  },
  "type": "module",
  "main": "./index.js",
  "exports": {
    ".": "./index.js"
  },
  "scripts": {
    "install:test-dependencies:firefox": "PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm install && npx playwright install firefox",
    "install:test-dependencies:chrome": "PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm install && npx playwright install chrome",
    "install:test-dependencies:all": "npm install && npx playwright install",
    "lint": "node ./node_modules/.bin/eslint \"*.{js,ts}\" \"utils/**/*.{js,ts}\" \"tests/**/*.{js,ts}\"",
    "prepare": "husky",
    "start:test-server": "node ./tests/testing-server.ts",
    "test": "npx bddgen && npx playwright test",
    "tsc": "node ./node_modules/typescript/bin/tsc -p ./",
    "patch": "npm version patch -m \"Bump up package version to %s\" && git push && git push origin --tags && npm publish",
    "minor": "npm version minor -m \"Bump up package version to %s\" && git push && git push origin --tags && npm publish",
    "major": "npm version major -m \"Bump up package version to %s\" && git push && git push origin --tags && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Marketionist/playwright-cucumber-steps.git"
  },
  "keywords": [
    "playwright cucumber",
    "playwright-cucumber",
    "playwright cucumber steps",
    "playwright-cucumber-steps",
    "cucumber steps",
    "cucumber step definitions",
    "playwright plugin",
    "automated tests",
    "e2e tests",
    "end-to-end testing",
    "acceptance testing",
    "browser testing",
    "playwright",
    "cucumber",
    "cucumberjs",
    "cucumber-js",
    "gherkin",
    "bdd"
  ],
  "author": "Dmytro Shpakovskyi",
  "license": "MIT",
  "peerDependencies": {
    "@playwright/test": ">=1.57.0",
    "playwright-bdd": ">=8.4.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.3.0",
    "eslint": "^10.0.1",
    "globals": "^17.3.0",
    "husky": "^9.1.7",
    "node-testing-server": "^1.6.1",
    "playwright-bdd": "^8.4.2",
    "typescript-eslint": "^8.56.0"
  }
}
